{
	"name": "archivebridge-extension",
	"version": "0.1.0",
	"private": true,
	"license": "MIT",
	"description": "ArchiveBridge browser extension: saves the current page as MHTML or Safari WebArchive. Chrome/Edge implemented; Firefox and Safari are planned first-class targets.",
	"type": "module",
	"scripts": {
		"build": "node build.mjs",
		"typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
		"test": "node --test \"src/**/*.test.ts\"",
		"test:e2e": "node --test --test-concurrency=1 --test-timeout=180000 \"e2e/**/*.test.ts\"",
		"lint": "biome check .",
		"format": "biome check --write .",
		"check": "npm run typecheck && npm run test && npm run lint && npm run build",
		"clean": "rm -rf dist"
	},
	"devDependencies": {
		"esbuild": "^0.28.2",
		"playwright": "^1.63.0"
	},
	"dependencies": {
		"@xarsh/archivebridge": "^0.0.1",
		"buffer": "^6.0.3",
		"string_decoder": "^1.3.0"
	}
}
